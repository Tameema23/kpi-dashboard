# ── KPI Dashboard — Python dependencies ───────────────────────────────────────
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database ORM
sqlalchemy==2.0.36

# ── Security packages ──────────────────────────────────────────────────────────

# bcrypt password hashing (replaces plain-text password storage)
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# JWT token creation and validation
python-jose[cryptography]==3.3.0

# Rate limiting (brute-force protection on /login)
slowapi==0.1.9

# Input validation (already used via pydantic, included for clarity)
pydantic==2.9.2
email-validator==2.2.0

# Timezone support (Python 3.9+ has zoneinfo built-in, this is the backport)
# Remove if using Python 3.9+
# backports.zoneinfo==0.2.1

# ── Optional: Postgres support ─────────────────────────────────────────────────
# Uncomment when you migrate from SQLite to Postgres (Railway, Render, etc.)
# psycopg2-binary==2.9.10